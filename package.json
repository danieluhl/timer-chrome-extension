{
  "name": "timer-ext",
  "version": "0.0.1",
  "description": "injects a timer into your page",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build"
  },
  "devDependencies": {
    "webextension-toolbox": "latest"
  }
}
